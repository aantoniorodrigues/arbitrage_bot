// Used as an example to make sure the tests were configured and running correctly

describe("computePriceDifference", () => {
  it("should be called periodically", () => {
    // TODO: Implement actual test
    const result = 3;
    const expected = 3;
    expect(result).toBe(expected);
  });
});
